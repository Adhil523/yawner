# Analysis report

_Generated by `python -m analysis.report` from the JSON in `build/`. Re-run the pipeline rather than editing by hand._

## Source clip

| Clip | Size | FPS | Frames | Duration | Codec | Audio |
|---|---|---|---|---|---|---|
| `Man_yawning_on_white_background_202609071453.mp4` | 1080×1920 | 24/1 | 240 | 10.00 s | h264 / yuv420p | yes (stripped) |

## Flags

- Man_yawning_on_white_background_202609071453.mp4: has an audio track (normalize strips it)
- only 2.7 s of still footage (start 1.5 s + end 1.2 s); the brief asks for at least 3 s, so the idle loop will be short
- best end-to-start jump costs 2.7x the natural drift over 8 frames; it will need a crossfade bridge
- entry cut 239 -> 36 costs 3.2x natural drift; review its jump sheet

## Segments

Normalised clip `build/normalized/Man_yawning_on_white_background_202609071453.mkv`: 240 frames at 24 fps.

| Part | Frames | Length |
|---|---|---|
| Still start | 0–35 | 1.50 s |
| Yawn | 36–210 | 7.29 s |
| Still end | 211–239 | 1.21 s |

Yawn = smoothed motion above 0.348 (still level 0.268).
Boundaries corrected by hand in `analysis/config.py` (`SEGMENT_OVERRIDES`: {'onset': 36}); auto-detected yawn was 21–210.
Motion plot: [`motion.png`](../build/preview/motion.png). Frames with segment colours (green start, red yawn, blue end): [`contact_sheet.png`](../build/preview/contact_sheet.png).

## Natural drift in the still parts

Mean absolute difference (grey levels, 0–255) between frames `gap` apart, on blurred 160px-tall descriptors. This is the yardstick for seam costs.

| Gap (frames) | Seconds | Mean change |
|---|---|---|
| 1 | 0.04 | 0.275 |
| 2 | 0.08 | 0.445 |
| 4 | 0.17 | 0.778 |
| 8 | 0.33 | 1.308 |
| 12 | 0.50 | 1.601 |
| 16 | 0.67 | 1.736 |
| 24 | 1.00 | 1.938 |

## Best end → start cuts

Windowed cost (brief 2c) of showing still-end frame *i*, then still-start frame *j*.

| Cut | Cost | × drift over 8 frames |
|---|---|---|
| 239 → 30 | 3.497 | 2.7× |
| 239 → 29 | 3.504 | 2.7× |
| 238 → 29 | 3.515 | 2.7× |
| 239 → 31 | 3.518 | 2.7× |
| 238 → 30 | 3.519 | 2.7× |

Best cut vs natural drift, amplified ×8: [`seam_difference.png`](../build/preview/seam_difference.png).

## Idle loop

Frames 219–239, 0.88 s per cycle. The last 8 frames crossfade into the frames just before 219, then playback carries on at 219.
Seam: cut 239 → 219, cost 0.908 = 0.7× natural drift over 8 frames.

## Entries (idle → yawn)

| Cut | How | Cost | × drift | Taking it → first yawn frame |
|---|---|---|---|---|
| 239 → 36 | 8-frame crossfade (`bridge_entry_0`) | 4.153 | 3.2× | 0.33 s |

Worst case from a debounced arrival to the first yawn frame: 1.21 s (target 1.5 s). The yawn then always plays to the end.

## Exits (yawn → idle)

| Cut | How | Cost | × drift |
|---|---|---|---|
| 218 → 219 | plays straight on, no cut | 0.000 | – |

## Simulated run

[`preview.mp4`](../assets/preview/preview.mp4): 75 s, 5 scripted visits (seed 7), using the debounce and behaviour settings in `player/config.toml`. The corner label shows the player state.

| Arrived | Left | Arrival → first yawn frame |
|---|---|---|
| 2.00 s | 4.62 s | 0.62 s |
| 15.65 s | 19.90 s | 0.72 s |
| 30.66 s | 34.33 s | 1.21 s |
| 46.11 s | 47.40 s | 1.27 s |
| 59.68 s | 60.87 s | 1.45 s |

Jumps taken: `bridge_loop` ×37, `bridge_entry_0` ×5, `straight on 218->219` ×5. Arrival times include the presence debounce.

Close-ups of each crossfade (3 frames before, the fade, 3 frames after; below it, the hard cut it replaces):
- [`jump_bridge_loop.png`](../build/preview/jump_bridge_loop.png)
- [`jump_bridge_entry_0.png`](../build/preview/jump_bridge_entry_0.png)
